spring.application.name=TicketBookingSystem

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ticket_system?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Tharun21$
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate/JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Redis Configuration (DISABLED FOR PHASE 2)
# spring.data.redis.host=localhost
# spring.data.redis.port=6379

# Kafka Configuration (DISABLED FOR PHASE 2)
# spring.kafka.bootstrap-servers=localhost:9092
# spring.kafka.consumer.group-id=ticket-group
# spring.kafka.consumer.auto-offset-reset=earliest